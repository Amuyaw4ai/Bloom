# Bloom
Bloom is an adaptive React cycle tracker that moves away from static 28-day models. It uses custom formulas to dynamically predict ovulation &amp; a 7-day unsafe window. Features real-time symptothermal tracking (BBT &amp; mucus), persistent localStorage, and elegant Tailwind visuals.
